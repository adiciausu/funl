max_redirects: 10
max_errors: 25
delay_factor: 2 #next_delay = pow(delay_factor, current_error_count);
default_request_ttl: 10 #in seconds;[integer()| none]; none => never expires; overriden by request X-Funl-Ttl header
endpoint: http://arequestb.in/116kf3t1
route_strategy:  all_to_endpoint
backend_max_req: 100 # per second


